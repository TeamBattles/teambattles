// Public entry point for the TeamBattles TypeScript SDK.
//
// NOTE: the generated client module path is produced by Kiota in CI; adjust the re-export path/name
// below after the first generation if it differs.

export { createTeamBattlesClient } from "./client.js";
export { createTeamBattlesApiClient } from "./generated/teamBattlesApiClient.js";
