# frozen_string_literal: true

# Public entry point for the TeamBattles Ruby SDK.
# Requires the convenience wrapper (which in turn requires the generated client).

require_relative "teambattles_sdk/client"

module TeamBattlesSdk
  VERSION = "0.1.0"
end
